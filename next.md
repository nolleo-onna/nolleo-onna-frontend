# next.md — 다음 세션이 먼저 읽는 파일

> **새 세션은 작업 전에 이 파일을 먼저 읽는다.** 어디까지 했고 다음에 뭘 할지만 적는다.
> 지나간 기록은 [PROGRESS.md](./PROGRESS.md)에 쌓고, 여기엔 "지금"만 남긴다.
> 작업을 끝내면 이 파일을 갱신하고 커밋한다.

마지막 갱신: 2026-09-19

---

## 지금 상태

| 항목 | 값 |
|---|---|
| 기본 브랜치 | `main` (배포), `develop` (통합) |
| 마지막 릴리스 | **1.14.0** (#145, main 머지 완료) |
| main에 안 나간 것 | 파도 배경 높이 수정(#146) → **릴리스 PR #148 열려 있음** |
| 작업 중 브랜치 | `feat/public-courses-all` (푸터 연도 · 공유 코스 전체보기 · 문서) |

## 지금 막힌 것 (사람이 해야 함)

1. **릴리스 PR #148(Release 1.14.1) 머지** — `main`은 승인 1개가 필요하고 본인 PR은 본인이 승인할 수 없다.
   터미널에서 직접: `! gh pr merge 148 --merge --admin --subject "Release 1.14.1 (#148)"`
   ⚠️ **squash 말고 merge commit으로.** squash하면 다음 릴리스 전에 sync PR(#147 같은 것)이 또 필요하다.
2. **백엔드 `POST /api/v1/courses` 배포** — 아직이라 실서비스에서 "코스 만들기"가 동작하지 않는다.
   프론트는 404/501이면 "아직 서버에 준비되지 않았어요"로 안내만 한다.

## 다음 할 일 (우선순위)

1. **행사가 코스에 안 들어가는 문제** — 사용자 확인됨. 논의만 하고 아직 구현 안 함.
   - 원인: 행사(TourAPI 이벤트)는 장소 데이터(`/api/v1/map/places`)에 없다. 실제로 `불꽃`·`축제`·`영화제` 검색 모두 0건.
     폼의 `festival`은 **코스 검색 중심 좌표만** 바꾼다(스펙: "찾으면 축제 좌표가 코스 검색 중심이 된다").
   - 정한 방향: **(1) 프론트 임시** 결과 화면 맨 위에 행사 카드를 고정으로 띄우기(포스터·기간·위치·길찾기).
     사용자가 고른 행사의 `contentId`를 `pairId`와 함께 저장해 두고 결과 페이지가 이벤트 상세를 부른다.
     한계: 보여주기만 — 저장된 코스·공유 링크·편집 화면엔 안 들어간다.
     **(2) 백엔드 정식** 코스 아이템에 행사를 포함(`placeType: "EVENT"`, 이벤트 contentId·좌표·기간).
   - 사용자에게 (1)을 만들지, 백엔드 요청서(2)를 써줄지 물어본 상태 — **답을 기다리는 중**.
2. 홈 히어로 A-2 디자인 후속 요청이 오면 `features/home/components/HeroSection/aiFirst/courseForm/`에서 작업.
3. 스토리북에만 있는 시안(B·D·E·F, 조건 폼 A-1·A-3·A-4)을 정리할지 결정 — 지금은 비교용으로 남겨둠.

## 작업 규칙 (사용자 지시)

- 문제 하나 = PR 하나. **PR 올리고 develop까지 머지는 내가(=어시스턴트) 한다.**
- **main 머지는 사용자가 한다.** 릴리스 PR만 만들어 두고 알린다.
- 릴리스 PR이 squash로 머지되면 `git merge -s ours origin/main` sync PR을 만들어 develop 이력을 잇는다 (merge commit으로 머지).
- 커밋 제목은 대문자로 시작하면 commitlint가 막는다 (예: "AI …" → 다른 말로 바꾼다).
- 작업 전 `git fetch`로 최신 `origin/develop`에서 브랜치를 딴다.

## 환경 메모

- 개발 API(`api-dev.nolleo-onna.site`)는 이 샌드박스·브라우저에서 안 열린다. 운영 API(`api.nolleo-onna.site`)는 브라우저에서 조회된다.
  그래서 스토리북 스토리 일부는 운영 API에서 받아 둔 실제 응답으로 `fetch`를 흉내 낸다.
- 스토리북: `pnpm storybook` (이 워크트리는 6007 포트에서 이미 떠 있는 경우가 많다).
- 브라우저 탭이 백그라운드라 `ResizeObserver`·모션이 멈춘다. 스크린샷을 찍으면 한 번 그려진다.
- 검사: `pnpm exec tsc --noEmit -p .` · `pnpm exec eslint <경로>` · `pnpm test` · `pnpm build`
