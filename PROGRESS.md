# PROGRESS.md — 진행 기록

> 이 프로젝트에서 무엇을 왜 했는지 쌓아두는 파일. "다음에 뭘 할지"는 [next.md](./next.md)에 적는다.
> 새로 끝낸 작업은 맨 위 섹션에 한 줄씩 추가한다 (날짜 · PR 번호 · 왜).

프로젝트: **놀러온나** — 부산 여행 코스 웹앱 (Next.js 15 App Router · TS · Tailwind v4 · TanStack Query)

---

## 릴리스

| 릴리스 | PR | 내용 |
|---|---|---|
| 1.14.1 | #148 | (열림) 히어로가 늘어나면 파도 배경도 같이 늘어나게 |
| 1.14.0 | #145 | 새 홈 히어로(채팅 우선 + 한 줄 조건 바), 스팟 지도 이동·마커, 행사 포스터 링크 |
| 1.13.1 | #139 | 코스 생성 실패 시 서버 원문 메시지 노출 차단 |
| 1.13.0 | #137 | 폼으로 코스 만들기(`POST /api/v1/courses`) 연동 + 장소·행사 검색 |

`main`이 squash로 머지되면 develop 이력을 잇는 sync PR을 따로 만든다 (#140, #147).

---

## 2026-09 작업

### 코스 카드 노선도형으로 교체 (#150, #151, #152)
- 시안 3종(A 매거진 · B 코스 티켓 · C 노선도)을 스토리북에서 비교해 처음엔 A를 적용했다가 **C 노선도**로 최종 결정
- 홈 "지금 인기 있는 코스"와 `/course/shared` 전체보기에 적용, 기존 여정형 카드(RouteItineraryCard) 제거
- A·B는 `Course/코스 카드 시안`에 비교용으로 남겨둠

### 공유된 코스 전체보기 · 푸터 (#149)
- 푸터 저작권 연도 2025 → 2026
- 홈 "지금 인기 있는 코스" 오른쪽 링크를 **전체보기**로 바꾸고 `/course/shared` 페이지 추가
  - 격자 목록 + 정렬(인기순 · 최신순 · 좋아요순) + 더 보기. 카드는 홈과 같은 여정형 카드 재사용
  - `GET /api/v1/courses/popular`에 `page` 파라미터가 먹는 것을 확인하고 무한 스크롤 훅(`usePublicCourses`)으로 감쌈
- `PROGRESS.md` · `next.md` 추가, `CLAUDE.md`에 "작업 전 next.md 읽기" 규칙 추가

### 홈 히어로 개편 (#144, #146 → 릴리스 1.14.0/1.14.1)
- 피드백: 온나 채팅이 메인인데 검색 카드 구석 버튼이라 안 보인다 → **1순위 채팅, 2순위 조건 폼**으로 다시 설계
- 스토리북에서 시안 비교(A·B·D·E·F) 후 **A(가운데 큰 채팅창)** 선택, 조건 폼은 4종(문장형·한 줄 바·탑승권·단계형) 중 **A-2 한 줄 검색 바** 선택
- 한 줄 바: 어디서·예산 **필수**(안 고르면 빨갛게 표시하고 판 열기), 꼭 갈 곳·행사 **선택**,
  지역 → 예산 → 꼭 갈 곳으로 판이 이어짐, 지역 카드(이모지·한 줄 소개),
  꼭 갈 곳은 스팟 장소 이름 일부 검색(`이재` → 이재모피자 본점) + 치기 전엔 부산 전체 추천(사진·분류 탭),
  행사는 치기 전엔 지금·곧 열리는 행사(D-day 배지)
- 코스 생성 로직을 `useGenerateCourse` 훅으로 분리해 기존 `SearchBar`와 공유
- vanta 파도 캔버스가 창 크기 변화만 따라가서, 판이 펼쳐져 히어로가 길어지면 아래쪽이 배경색만 보이던 문제 → `ResizeObserver`로 수정

### 스팟·홈 피드백 (#141, #142, #143)
- 홈 행사 포스터 클릭 → 그 행사의 한끗 정보로 이동 (없으면 동네 한끗 → 행사 상세)
- 스팟 오른쪽 목록 검색창에 한 글자 치면 입력이 끊기던 문제 (`keepPreviousData`로 목록 언마운트 방지)
- 스팟 목록에서 장소를 고르면 지도가 그 자리로 이동하고 마커가 흔들린다. 상세는 마커·"상세정보" 버튼으로

### 공모전 기능설명서 (레포 밖)
- 구글 슬라이드 6~10쪽에 기능 흐름도 텍스트 입력 (이미지는 사용자가 추후 추가)

---

## 알려진 제약

- **백엔드 `POST /api/v1/courses` 미배포** — 실서비스에서 폼 코스 생성이 아직 동작하지 않는다
- **행사는 코스에 들어가지 않는다** — 행사 데이터가 장소 데이터에 없고, 폼의 `festival`은 검색 중심 좌표만 바꾼다 (next.md 참고)
- `main` 브랜치는 승인 1개 필요 — 릴리스 PR 머지는 사용자가 한다
